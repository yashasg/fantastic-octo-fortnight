# UI Test README
## EyePostureReminderUITests

> **Status:** Re-enabled in CI after the MVVM → TCA migration ([#662](https://github.com/yashasg/fantastic-octo-fortnight/issues/662)) shipped. The `uitest-prepare`, `uitest-shard`, and `uitest` jobs in
> `.github/workflows/ci.yml` run on every pull request and `main` push under
> issue [#778](https://github.com/yashasg/fantastic-octo-fortnight/issues/778).

### Triage decision (#778)

After the TCA migration, every remaining XCUITest file was kept rather than
ported down to `TestStore`. The suite exercises the app *from the outside*
through accessibility identifiers and launch arguments (`UITestHelpers.swift`);
none of the assertions depend on MVVM- or TCA-specific internals, so the
existing coverage transferred unchanged. Per-feature `TestStore` tests under
`Tests/EyePostureReminderTests/TCA/` cover the reducer logic separately.

Tests with known regressions or flakes are wrapped in `XCTExpectFailure` and
linked to a tracking issue — drop the wrapper once the underlying fix lands:

| Test | Tracking issue | Reason |
|---|---|---|
| `OverlayPresentationTests.test_overlay_settingsLink_opensSettingsWithSnoozeOptions` | [#786](https://github.com/yashasg/fantastic-octo-fortnight/issues/786) | TCA regression: overlay → Settings sheet routing dropped |
| `SettingsFlowTests.test_settings_savedBanner_appearsOnToggle` | [#787](https://github.com/yashasg/fantastic-octo-fortnight/issues/787) | XCUI flake: transient banner not discoverable in CI |

### Running locally

```bash
./scripts/build.sh uitest                                                 # full suite
./scripts/build.sh uitest --only-testing EyePostureReminderUITests/HomeScreenTests
./scripts/build.sh uitest --only-testing EyePostureReminderUITests/HomeScreenTests --result-bundle-path UITestResults-home.xcresult
```

The UITest target is auto-generated by XcodeGen — `scripts/build.sh uitest`
runs `scripts/setup-uitests.sh` if the project is missing or stale.

### Test Files

| File | Suite |
|---|---|
| `OnboardingFlowTests.swift` | Welcome / Permission / Setup / Interrupt-mode screens, including the Customize Settings → auto-open path |
| `SettingsFlowTests.swift` | Master toggle, Smart Pause, snooze, legal sheets, reset/feedback rows |
| `HomeScreenTests.swift` | Home anchors, settings handoff, True-Interrupt skipped banner & setup pill |
| `OverlayTests.swift` | Eye/posture overlay presentation, Done/Settings-link dismissal |
| `AppStoreScreenshotTests.swift` | Opt-in screenshot capture for App Store submission assets |
| `UITestHelpers.swift` | Shared `XCUIApplication` helpers + `TestLaunchArguments` |

### Naming Convention

Test methods follow the `test_screen_action_expectedResult` pattern, consistent with the unit test suite.

### Launch Arguments

Tests use `launchArguments` to control app state via helpers in `UITestHelpers.swift`:

| Constant | Argument | Effect |
|---|---|---|
| `TestLaunchArguments.skipOnboarding` | `--skip-onboarding` | Sets `hasSeenOnboarding = true` before launch → app opens on Home screen |
| `TestLaunchArguments.resetOnboarding` | `--reset-onboarding` | Clears `hasSeenOnboarding` → app starts with onboarding flow |
| `TestLaunchArguments.showOverlayEyes` | `--show-overlay-eyes` | Triggers eye break overlay on launch |
| `TestLaunchArguments.showOverlayPosture` | `--show-overlay-posture` | Triggers posture check overlay on launch |

**These arguments are handled in `AppDelegate.swift`** via `applyUITestLaunchArguments()` called from `application(_:didFinishLaunchingWithOptions:)`.

Use the `XCUIApplication` convenience helpers instead of raw strings:

```swift
// In setUpWithError():
app = XCUIApplication()
app.launchWithSkippedOnboarding()  // or app.launchWithOnboarding()
app.launchWithEyeOverlay()
```

### Accessibility Identifiers

#### OnboardingWelcomeView
- Body/disclaimer `Text` → `"onboarding.welcome.disclaimer"` ✅
- Next `Button` → `"onboarding.welcome.nextButton"` ✅

#### OnboardingPermissionView
- Skip/next `Button` → `"onboarding.permission.nextButton"` ✅
- Allow notifications `Button` → `"onboarding.enableNotifications"` ✅

#### OnboardingSetupView
- Get Started `Button` → `"onboarding.setup.getStartedButton"` ✅
- Change in settings reassurance `Text` → `"onboarding.setup.changeInSettings"` ✅
- Eyes interval `Picker` → `"onboarding.eyes.intervalPicker"` ✅
- Eyes break duration `Picker` → `"onboarding.eyes.durationPicker"` ✅
- Posture interval `Picker` → `"onboarding.posture.intervalPicker"` ✅
- Posture break duration `Picker` → `"onboarding.posture.durationPicker"` ✅

#### OnboardingInterruptModeView
- Skip `Button` → `"onboarding.interrupt.skipButton"` ✅
- Enable/setup preview `Button` → `"onboarding.interrupt.enableButton"` ✅
- Customize `Button` → `"onboarding.interrupt.customizeButton"` ✅
- Summary card `View` → `"onboarding.interrupt.summaryCard"` ✅
- Pending permission badge `View` → `"onboarding.interrupt.pendingBadge"` ✅

#### AppCategoryPickerView
- Unavailable banner → `"appCategoryPicker.unavailableBanner"` ✅
- Primary action `Button` → `"appCategoryPicker.primaryButton"` ✅
- Done `Button` → `"appCategoryPicker.doneButton"` ✅
- Pre-permission card `View` → `"appCategoryPicker.prePermissionCard"` ✅
- Denied permission card `View` → `"appCategoryPicker.deniedCard"` ✅
- Approved permission card `View` → `"appCategoryPicker.approvedCard"` ✅

#### HomeView
- Status icon (`YinYangEyeView`) — decorative, `.accessibilityHidden(true)`, no identifier exposed
- Title `Text` → `"home.title"` ✅
- Status label `Text` → `"home.statusLabel"` ✅
- Settings toolbar `Button` → `"home.settingsButton"` ✅
- True-interrupt setup pill `Button` → `"home.trueInterrupt.setupPill"` ✅
- Skipped-banner set-up `Button` → `"home.trueInterrupt.skippedBanner.setUp"` ✅
- Skipped-banner dismiss `Button` → `"home.trueInterrupt.skippedBanner.dismiss"` ✅

#### SettingsView
- Done toolbar `Button` → `"settings.doneButton"` ✅
- Master on/off `Toggle` → `"settings.masterToggle"` ✅
- Focus Mode `Toggle` → `"settings.smartPause.pauseDuringFocus"` ✅
- Driving `Toggle` → `"settings.smartPause.pauseWhileDriving"` ✅
- Haptic feedback `Toggle` → `"settings.hapticFeedback"` ✅
- Notification fallback `Toggle` → `"settings.notificationFallback"` ✅
- Terms row `Button` → `"settings.legal.terms"` ✅
- Privacy row `Button` → `"settings.legal.privacy"` ✅
- Legal disclaimer row `Button` → `"settings.legal.disclaimer"` ✅
- Snooze cancel `Button` → `"settings.snooze.cancelButton"` ✅
- Snooze 5 min `Button` → `"settings.snooze.5min"` ✅
- Snooze 1 hour `Button` → `"settings.snooze.1hour"` ✅
- Snooze rest of day `Button` → `"settings.snooze.restOfDay"` ✅
- Reset to defaults `Button` → `"settings.resetToDefaults"` ✅
- Send feedback `Button` → `"settings.feedback.sendFeedback"` ✅
- True-interrupt status row `View` → `"settings.trueInterrupt.statusRow"` ✅
- True-interrupt open Screen Time settings `Button` → `"settings.trueInterrupt.openSettings"` ✅
- True-interrupt configure `Button` → `"settings.trueInterrupt.configureButton"` ✅
- Notifications open settings `Button` → `"settings.notifications.openSettings"` ✅

#### LegalDocumentView
- Dismiss `Button` → `"legal.dismissButton"` ✅

#### OverlayView
- Overlay root container `ZStack` → `"overlay.root"` ✅
- × dismiss `Button` → `"overlay.dismissButton"` ✅
- Done CTA `Button` → `"overlay.doneButton"` ✅
- Supportive subtitle `Text` → `"overlay.supportiveText"` ✅
- Settings link `Button` → `"overlay.settingsLink"` ✅
